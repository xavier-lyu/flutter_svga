name: flutter_svga
description: "A Flutter package for rendering SVGA animations with dynamic customization, smooth playback, and high performance. Actively maintained SVGA player for Flutter with intelligent caching, audio support, and dynamic elements. Modern replacement for archived svgaplayer_flutter."
version: 0.0.11

homepage: https://github.com/5alafawyyy/flutter_svga
repository: https://github.com/5alafawyyy/flutter_svga

topics:
  - animation
  - svga
  - vector-graphics
  - lottie-alternative
  - ui-effects

environment:
  sdk: ^3.10.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  archive: ^4.0.7
  http: ^1.6.0
  path_drawing: ^1.0.1
  protobuf: ^6.0.0
  just_audio: ^0.10.5
  path_provider: ^2.1.5
  crypto: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
